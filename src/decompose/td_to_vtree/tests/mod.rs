//! Beside the module: these reach `BagMetadata::from_assignment`, the
//! `pub(super)` clause-length cap the co-occurrence order reads, and the
//! construction meter. The conversion over its public entry points is tested
//! in `src/tests/decompose/td_to_vtree/`.

mod convert;
mod meta;
mod search;
