//! Configuration tested through the readers `config` keeps to itself. The
//! shape of a `RunConfig` and everything a caller can reach is tested from
//! `src/tests/config.rs` instead.

mod env_defaults;
